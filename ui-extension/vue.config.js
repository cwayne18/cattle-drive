// Rancher Dashboard loads extensions via its own Nuxt/Webpack pipeline.
// Extension-specific vue.config.js customisation is not needed here;
// this file is required by the scaffold so that the build tooling picks up
// the extension package correctly.
module.exports = {};
